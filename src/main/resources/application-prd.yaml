spring:
  application:
    name: santander-dev-week-2023
    datasource:
      url: jdbc:postgresql://${PG_HOST}:${PG_PORT}/${PG_DATABASE}
      username: ${PG_USER}
      password: ${PG_PASSWORD}
    jpa:
      open-in-view: false
      hibernate:
        ddl-auto: validate